<!-- project: 260709_App David -->
<!-- status: aktiv -->
# E1 Material — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Eine mobile PWA, mit der Monteure auf der Baustelle leeres Fahrzeug-Material antippen und daraus eine Nachbestell-Mail erzeugen.

**Architecture:** Reine Browser-PWA (kein Build, kein Server), gespiegelt vom Schwesterprojekt `260225_Datenaufnahme`. Materialkatalog aus einer vom Monteur in Excel gepflegten `MatCombo.xlsx` (SheetJS-Import), Erfassung in einen lokalen Nachbestell-Korb (IndexedDB, inkl. optionaler Fotos), Versand ueber `mailto` + ZIP-Anhang.

**Tech Stack:** HTML, CSS, Vanilla-JS (ES-Module oder klassisch wie im Vorbild), SheetJS (`lib/xlsx.mini.min.js`), Service Worker, IndexedDB, `localStorage`. Ziel-Host: GitHub Pages.

## Global Constraints

- **Vorbild verbindlich:** Optik, Header, Einstellungs-Mechanik, Service-Worker-Muster (Network-first), ZIP+`mailto`-Export 1:1 an `260225_Datenaufnahme` ausrichten. Vor jeder Implementierung die entsprechende Vorbild-Datei lesen.
- **App-Name:** `E1 Material` (in `manifest.json` `name` + `short_name`, im Header, im `<title>`).
- **Deutsche Umlaute** im UI-Text (ae/oe/ue nur in Bezeichnern/Dateinamen, nie im sichtbaren Text).
- **Kein Server, keine Anmeldung, keine Datenuebertragung** nach aussen; alle Daten lokal.
- **Kein Build-Anstoss aus Claude** (PyInstaller/npm build etc. bleibt bei Felix). Reine Datei-Erstellung/-Bearbeitung.
- **Theme:** `theme_color` `#1565C0`, `background_color` `#F5F5F5` (wie Vorbild), sofern nicht bewusst geaendert.
- **Changelog-Pflicht (E1):** sichtbare Verhaltensaenderungen in `CHANGELOG.md` fuehren; Version daraus.
- **Vorbild-Pfad:** `C:/Users/Felix.Rundel/OneDrive - E1 Energiemanagement GmbH/2 Buero/6 GPT_Python/260225_Datenaufnahme`
- **Projekt-Pfad:** `C:/Users/Felix.Rundel/OneDrive - E1 Energiemanagement GmbH/2 Buero/6 GPT_Python/260709_App David`

**Verifikation ohne Test-Harness:** Das Vorbild ist eine build-freie PWA ohne Unit-Tests. Jede Task wird per Browser-Sichtpruefung verifiziert (`index.html` lokal oeffnen bzw. ueber `start.py`/einfachen HTTP-Server, Aktion ausfuehren, erwartetes Ergebnis pruefen — Service Worker verlangt `http://`-Kontext, nicht `file://`). Konsolen-Checks (`localStorage`, IndexedDB via DevTools) ergaenzen.

---

## File Structure

| Datei | Verantwortung |
|---|---|
| `index.html` | Single-Page-Shell, alle Ansichten, Skript-Einbindung |
| `css/style.css` | Optik, aus Vorbild uebernommen + angepasst |
| `js/app.js` | Bootstrapping, Ansichts-Routing, Einstellungen, Erststart |
| `js/catalog.js` | Excel-Import (SheetJS), Parsen, Katalog-Speicher, Suche/Vorschlaege |
| `js/basket.js` | Nachbestell-Korb: Posten hinzufuegen/editieren/entfernen, Rendern |
| `js/db.js` | IndexedDB-Wrapper (Korb-Posten + Foto-Blobs) |
| `js/export.js` | ZIP bauen (Liste + Fotos) + `mailto` erzeugen |
| `lib/xlsx.mini.min.js` | SheetJS (aus Vorbild kopiert) |
| `data/MatCombo.xlsx` | eingebaute Start-Materialliste |
| `manifest.json` | PWA-Manifest |
| `sw.js` | Service Worker (Network-first, aus Vorbild) |
| `version.json` | Versionsanzeige |
| `icons/icon-192.png`, `icons/icon-512.png` | App-Icons |
| `hilfe/index.html` | Kurz-Hilfe (In-App) |
| `README.md`, `CHANGELOG.md` | Doku / Versionshistorie |

---

## Block A — Geruest & PWA-Infrastruktur

### Task A1: Ordnerstruktur + statische Infrastruktur aus Vorbild spiegeln

**Files:**
- Create: `js/`, `css/`, `lib/`, `data/`, `icons/`, `hilfe/`
- Copy (aus Vorbild): `lib/xlsx.mini.min.js`, `icons/icon-192.png`, `icons/icon-512.png`
- Copy (Projekt→data): `MatCombo.xlsx` → `data/MatCombo.xlsx`
- Create: `version.json`

**Interfaces:**
- Produces: Verzeichnisbaum + `lib/xlsx.mini.min.js` (globales `XLSX`-Objekt), `data/MatCombo.xlsx`.

- [ ] **Step 1:** Verzeichnisse `js/ css/ lib/ data/ icons/ hilfe/` im Projektordner anlegen.
- [ ] **Step 2:** `lib/xlsx.mini.min.js` und beide `icons/icon-*.png` aus dem Vorbild kopieren.
- [ ] **Step 3:** `MatCombo.xlsx` aus dem Projekt-Root nach `data/MatCombo.xlsx` kopieren (Original im Root belassen).
- [ ] **Step 4:** `version.json` anlegen: `{"version":"v0.1.0"}`.
- [ ] **Step 5:** Sichtpruefung — Dateien vorhanden, `xlsx.mini.min.js` > 200 KB.

### Task A2: index.html-Shell, style.css, manifest.json, sw.js

**Files:**
- Create: `index.html`, `manifest.json`, `sw.js`
- Create: `css/style.css` (aus Vorbild `css/style.css` uebernommen)

**Interfaces:**
- Produces: App-Shell mit Header "E1 Material", leeren Ansichts-Containern (`#view-katalog`, `#view-korb`, `#view-einstellungen`), Navigations-Elementen, eingebundenem SW und Manifest.

- [ ] **Step 1:** Vorbild lesen: `260225_Datenaufnahme/index.html`, `css/style.css`, `manifest.json`, `sw.js` — Header-Aufbau, Versionsanzeige, Ansichts-Umschaltung, SW-Registrierung, Cache-Namen verstehen.
- [ ] **Step 2:** `css/style.css` aus dem Vorbild kopieren; App-spezifische Titel/Beschriftung nicht in CSS haerten.
- [ ] **Step 3:** `manifest.json` schreiben — `name`/`short_name` = "E1 Material", `description` = "E1 Energiemanagement — Materialnachbestellung aus dem Fahrzeug", `start_url` `./index.html`, `display` `standalone`, `theme_color` `#1565C0`, `background_color` `#F5F5F5`, beide Icons.
- [ ] **Step 4:** `sw.js` aus Vorbild uebernehmen; Cache-Namen auf `e1-material-v0.1.0` umbenennen, Precache-Liste an die Dateien dieses Projekts anpassen (index.html, css, js/*, lib, data/MatCombo.xlsx, manifest, icons).
- [ ] **Step 5:** `index.html` schreiben: `<title>E1 Material</title>`, Header mit App-Name + Versionsanzeige (aus `version.json`), drei leere Ansichts-Container, Fuss-/Tab-Navigation "Katalog | Korb | Einstellungen", SW-Registrierung, Einbindung `lib/xlsx.mini.min.js` + `js/db.js` + `js/catalog.js` + `js/basket.js` + `js/export.js` + `js/app.js`.
- [ ] **Step 6:** Lokalen HTTP-Server starten (z.B. `py -3.13 -m http.server 8000` im Projektordner) und `http://localhost:8000` oeffnen.
  Erwartet: Header "E1 Material" + Version sichtbar, drei per Navigation umschaltbare (noch leere) Ansichten, SW registriert (DevTools → Application → Service Workers), Manifest ohne Fehler, App installierbar.
- [ ] **Step 7:** Commit (siehe Git-Hinweis in Task E3; bis Repo existiert, Commits sammeln).

---

## Block B — Materialkatalog (Import, Anzeige, Suche)

### Task B1: catalog.js — Excel parsen + Katalog-Speicher

**Files:**
- Create: `js/catalog.js`

**Interfaces:**
- Consumes: globales `XLSX` (SheetJS), `data/MatCombo.xlsx`.
- Produces:
  - `Catalog.parseWorkbook(arrayBuffer) -> Array<Item>` mit
    `Item = {kategorie, position, material, type, menge_soll, menge_num|null, bemerkung}`
  - `Catalog.load() -> Promise<Array<Item>>` (aus `localStorage` `e1mat_catalog`, sonst eingebaute `data/MatCombo.xlsx` per fetch)
  - `Catalog.save(items)` (nach `localStorage`)
  - `Catalog.groupByKategorie(items) -> Map<string, Array<Item>>`

- [ ] **Step 1:** `js/catalog.js` anlegen. Parser: Workbook lesen, Sheet `Tabelle1`, Kopfzeile ist die **dritte Zeile** (Index 2). Zeilen ohne `Material` verwerfen. `Menge` numerisch parsen (Komma→Punkt, sonst `menge_num=null`, `menge_soll` = Rohtext wie "i.o"). Zellen defensiv gegen `undefined`/Umlaute lesen.

```js
const Catalog = (() => {
  const KEY = 'e1mat_catalog';
  const HEADER_ROW = 2; // 0-indexiert: Kopfzeile in Excel-Zeile 3

  function parseWorkbook(arrayBuffer) {
    const wb = XLSX.read(arrayBuffer, { type: 'array' });
    const ws = wb.Sheets[wb.SheetNames[0]];
    const rows = XLSX.utils.sheet_to_json(ws, { header: 1, defval: '' });
    const items = [];
    for (let i = HEADER_ROW + 1; i < rows.length; i++) {
      const r = rows[i];
      const material = String(r[2] ?? '').trim();
      if (!material) continue;
      const mengeRaw = String(r[4] ?? '').trim();
      const num = Number(mengeRaw.replace(',', '.'));
      items.push({
        kategorie: String(r[0] ?? '').trim() || 'Sonstiges',
        position: String(r[1] ?? '').trim(),
        material,
        type: String(r[3] ?? '').trim(),
        menge_soll: mengeRaw,
        menge_num: Number.isFinite(num) && mengeRaw !== '' ? num : null,
        bemerkung: String(r[5] ?? '').trim(),
      });
    }
    return items;
  }

  function save(items) { localStorage.setItem(KEY, JSON.stringify(items)); }

  async function load() {
    const cached = localStorage.getItem(KEY);
    if (cached) return JSON.parse(cached);
    const buf = await fetch('data/MatCombo.xlsx').then(r => r.arrayBuffer());
    const items = parseWorkbook(buf);
    save(items);
    return items;
  }

  function groupByKategorie(items) {
    const m = new Map();
    for (const it of items) {
      if (!m.has(it.kategorie)) m.set(it.kategorie, []);
      m.get(it.kategorie).push(it);
    }
    return m;
  }

  return { parseWorkbook, load, save, groupByKategorie, KEY };
})();
```

- [ ] **Step 2:** Verifikation im Browser-Konsolenkontext: `await Catalog.load()` liefert Array mit ~293 Eintraegen; `Catalog.groupByKategorie(await Catalog.load()).get('Elektroinstallation').length` ≈ 109.
- [ ] **Step 3:** Randfaelle pruefen: Eintrag mit `menge_soll==='i.o'` hat `menge_num===null`; Eintrag "Betonbohrer / 16er lang" vorhanden.

### Task B2: Katalog-Ansicht rendern (nach Kategorie gruppiert)

**Files:**
- Modify: `js/catalog.js` (Render-Funktion) und `index.html`/`js/app.js` (Aufruf beim Ansichtswechsel)

**Interfaces:**
- Consumes: `Catalog.load`, `Catalog.groupByKategorie`, `Basket.addFromItem(item)` (Task C2).
- Produces: `Catalog.renderInto(containerEl)` — rendert aufklappbare Kategorie-Gruppen; jeder Posten ein antippbares Element mit Material, Type, Position, Soll-Menge und "+"-Aktion.

- [ ] **Step 1:** `Catalog.renderInto(el)` schreiben: pro Kategorie ein aufklappbarer Block (`<details>`/Accordion im Vorbild-Stil), darin je Item eine Zeile mit `material`, `type`, `Fach <position>`, `Soll: <menge_soll>` und Button "+ In Korb".
- [ ] **Step 2:** In `js/app.js` beim Wechsel auf die Katalog-Ansicht `Catalog.renderInto(document.querySelector('#view-katalog'))` aufrufen.
- [ ] **Step 3:** Sichtpruefung: Katalog-Ansicht zeigt alle Kategorien; Aufklappen listet Posten; Optik entspricht Vorbild-Listen.

### Task B3: Suche mit Autovervollstaendigung/Vorschlaegen

**Files:**
- Modify: `js/catalog.js`, `index.html` (Suchfeld)

**Interfaces:**
- Produces: `Catalog.search(query, items) -> Array<Item>` (case-insensitive, Umlaut-tolerant, sucht in `material`+`type`), `Catalog.renderSuggestions(inputEl, resultEl)`.

- [ ] **Step 1:** Suchfeld oben in `#view-katalog` einfuegen. `Catalog.search`: normalisieren (lowercase, `ae/oe/ue/ss` ↔ Umlaut tolerant), Substring-Match auf `material` + " " + `type`. Rangfolge: Treffer am Wortanfang zuerst.
- [ ] **Step 2:** `input`-Handler: ab 2 Zeichen Vorschlagsliste (max. 12) unter dem Feld; Klick auf Vorschlag → Katalog-Ansicht auf diesen Posten scrollen/oeffnen bzw. direkt "In Korb"-Dialog. Bei leerem Feld volle Gruppierung.
- [ ] **Step 3:** Sichtpruefung: "Beton" → Betonbohrer-Treffer; "belimo" (aus Kopfnotiz-Umfeld) bzw. "reihenklemme" liefern erwartete Posten; Umlaut-Test "muell" findet "Muelltueten"/"Mülltüten".

### Task B4: Import-Funktion + Vorlage-Download (Einstellungen)

**Files:**
- Modify: `js/catalog.js`, `js/app.js` (Einstellungs-Ansicht)

**Interfaces:**
- Consumes: `Catalog.parseWorkbook`, `Catalog.save`.
- Produces: `Catalog.importFromFile(file) -> Promise<{count}>`, `Catalog.downloadTemplate()`.

- [ ] **Step 1:** In `#view-einstellungen` Buttons "Materialliste importieren" (`<input type=file accept=".xlsx">`) und "Vorlage herunterladen".
- [ ] **Step 2:** `importFromFile`: Datei → `arrayBuffer` → `parseWorkbook`; bei 0 Treffern klare Fehlermeldung ("Struktur passt nicht — Kopfzeile in Zeile 3, Spalte 'Material' noetig"), sonst Bestaetigungsdialog "N Posten gefunden — Katalog ersetzen?" → `save` + Katalog neu rendern.
- [ ] **Step 3:** `downloadTemplate`: `data/MatCombo.xlsx` per fetch laden und als Download anbieten (Blob + `<a download>`).
- [ ] **Step 4:** Sichtpruefung: Re-Import der `MatCombo.xlsx` meldet ~293 Posten; Vorlage-Download liefert oeffenbare Excel.

---

## Block C — Nachbestell-Korb & Erfassung

### Task C1: db.js — IndexedDB fuer Korb-Posten (+ Foto-Blobs)

**Files:**
- Create: `js/db.js`

**Interfaces:**
- Produces (Promise-basiert, Muster aus Vorbild `js/db.js`):
  - `DB.open() -> Promise<IDBDatabase>` (DB `e1material`, Store `korb`, keyPath `id`)
  - `DB.addPosten(posten) -> Promise<id>`
  - `DB.updatePosten(posten) -> Promise<void>`
  - `DB.deletePosten(id) -> Promise<void>`
  - `DB.allPosten() -> Promise<Array<Posten>>`
  - `DB.clearKorb() -> Promise<void>`
  - `Posten = {id, quelle, kategorie, position, material, type, bemerkung_katalog, nachbestell_menge, notiz, foto_blob?, erfasst_am}`

- [ ] **Step 1:** Vorbild `260225_Datenaufnahme/js/db.js` lesen (IndexedDB-Muster, Versionierung, Promise-Wrapping) und analog `js/db.js` fuer Store `korb` schreiben. Foto als `Blob` speichern.
- [ ] **Step 2:** Sichtpruefung (Konsole): `await DB.addPosten({...})`, `await DB.allPosten()` liefert den Eintrag; nach `DB.clearKorb()` leer; Persistenz nach Reload gegeben (DevTools → Application → IndexedDB).

### Task C2: Posten aus Katalog in den Korb (Menge/Notiz/Foto)

**Files:**
- Create: `js/basket.js`
- Modify: `index.html` (Erfassungs-Dialog)

**Interfaces:**
- Consumes: `DB.addPosten`, `Catalog` (Item-Struktur).
- Produces: `Basket.addFromItem(item)` — oeffnet Erfassungs-Dialog (Nachbestell-Menge vorbelegt = `item.menge_num ?? 1`, Feld editierbar; optionale Notiz; optionaler Foto-Aufnahme-Button via `<input type=file accept=image/* capture=environment>`), speichert via `DB.addPosten`, aktualisiert Korb-Badge.

- [ ] **Step 1:** Erfassungs-Dialog (Modal im Vorbild-Stil) mit Feldern Menge (Zahl), Notiz (Text), Foto (optional). Vorbelegung Menge = `menge_num ?? 1`.
- [ ] **Step 2:** `Basket.addFromItem(item)`: Dialog fuellen, bei Bestaetigung `Posten` bauen (`id = crypto.randomUUID()`, `quelle:'katalog'`, `erfasst_am = new Date().toISOString()`), Foto als Blob uebernehmen, `DB.addPosten`, Badge/Zaehler erhoehen.
- [ ] **Step 3:** Sichtpruefung: Posten aus Katalog antippen → Dialog → speichern → Korb-Badge zaehlt hoch; Eintrag in IndexedDB inkl. Foto-Blob.

### Task C3: Freitext-Posten

**Files:**
- Modify: `js/basket.js`, `index.html`

**Interfaces:**
- Produces: `Basket.addFreitext()` — Dialog mit freien Feldern (Material Pflicht, Type, Kategorie optional, Menge, Notiz, Foto), speichert `Posten` mit `quelle:'freitext'`.

- [ ] **Step 1:** Button "Freitext-Posten" in `#view-katalog`/Korb; Dialog wie C2, aber Material/Type frei eingebbar, Kategorie-Vorschlag "Sonstiges".
- [ ] **Step 2:** Sichtpruefung: Freitext-Posten "Kabelbinder 200mm / Menge 1" landet im Korb, `quelle==='freitext'`.

### Task C4: Korb-Ansicht (editieren/entfernen) + Badge

**Files:**
- Modify: `js/basket.js`, `js/app.js`, `index.html`

**Interfaces:**
- Consumes: `DB.allPosten`, `DB.updatePosten`, `DB.deletePosten`.
- Produces: `Basket.renderInto(containerEl)`, `Basket.refreshBadge()`.

- [ ] **Step 1:** `Basket.renderInto`: alle Posten listen (Material, Type, Menge, Notiz, Foto-Thumbnail); je Posten "Bearbeiten" (Menge/Notiz/Foto) und "Entfernen".
- [ ] **Step 2:** `refreshBadge`: Anzahl Posten am "Korb"-Tab anzeigen; beim App-Start aufrufen.
- [ ] **Step 3:** In `js/app.js` beim Wechsel auf Korb-Ansicht rendern.
- [ ] **Step 4:** Sichtpruefung: Korb zeigt Posten; Menge editieren persistiert; Entfernen loescht (auch nach Reload).

---

## Block D — Einstellungen & Versand

### Task D1: Monteur-Name (Erststart-Pflicht) + Empfaengerliste

**Files:**
- Modify: `js/app.js`, `index.html`

**Interfaces:**
- Produces: `Settings.getMonteur()/setMonteur(name)`, `Settings.getEmpfaenger()/setEmpfaenger(list)` (`localStorage` `e1mat_settings`); Empfaenger = `Array<{name, mail}>`.

- [ ] **Step 1:** Beim ersten Start (kein `monteur_name`): Pflicht-Dialog "Dein Name" (Muster wie Vorbild-Erfasser), speichern in `localStorage`.
- [ ] **Step 2:** In `#view-einstellungen`: Monteur-Name aenderbar; Empfaengerliste verwalten (hinzufuegen/entfernen: Name + Mailadresse).
- [ ] **Step 3:** Sichtpruefung: Erststart erzwingt Namen; Empfaenger anlegen/entfernen persistiert nach Reload.

### Task D2: export.js — ZIP (Liste + Fotos) + mailto

**Files:**
- Create: `js/export.js`

**Interfaces:**
- Consumes: `DB.allPosten`, `Settings`, ggf. ZIP-Utility aus Vorbild `js/export.js`.
- Produces:
  - `Exporter.buildZip(posten, monteur) -> Promise<Blob>` (Nachbestell-Liste als `.xlsx` via SheetJS **oder** `.txt`, plus Fotos `foto_1.jpg`…)
  - `Exporter.buildMailto(posten, monteur, empfaengerMails) -> string` (mailto-URL mit Betreff + Body)

- [ ] **Step 1:** Vorbild `260225_Datenaufnahme/js/export.js` lesen — ZIP-Bau (welche Lib/Technik) und `mailto`-Zusammenbau uebernehmen. **Offener Punkt lt. Spec:** Beilage-Format (Excel wie Vorbild vs. schlichte Textliste) an der Vorbild-`export.js` ausrichten; im Zweifel Excel wie Vorbild.
- [ ] **Step 2:** `buildMailto`: Betreff `Materialnachbestellung — <Monteur> — <TT.MM.JJJJ>`; Body Kopf (Monteur, Datum) + Posten gruppiert nach Kategorie als Zeilen `- <Material> <Type> — Menge: <nachbestell_menge> [Notiz]` + Hinweis "Liste und Fotos als ZIP anbei — bitte manuell anhaengen". `encodeURIComponent` fuer Betreff/Body; `to=` = Empfaenger-Mails komma-getrennt.
- [ ] **Step 3:** `buildZip`: Posten in Tabelle (SheetJS → `.xlsx`) oder Textdatei + alle Foto-Blobs; Blob zurueckgeben.
- [ ] **Step 4:** Sichtpruefung: `buildMailto(...)` erzeugt korrekt kodierte URL; `buildZip(...)` erzeugt oeffenbares ZIP mit Liste + Fotos.

### Task D3: Versand-Flow (Empfaenger ankreuzen, senden, Korb leeren)

**Files:**
- Modify: `js/basket.js`, `js/export.js`, `index.html`

**Interfaces:**
- Consumes: `Exporter.buildZip`, `Exporter.buildMailto`, `Settings.getEmpfaenger`, `DB.clearKorb`.
- Produces: `Basket.sendNachbestellung()`.

- [ ] **Step 1:** Button "Nachbestellung senden" in Korb-Ansicht (deaktiviert bei leerem Korb). Dialog: Empfaenger ankreuzen (aus `Settings.getEmpfaenger`) + Feld fuer freie Zusatz-Adresse.
- [ ] **Step 2:** Bei Bestaetigung: ZIP bauen + herunterladen, `mailto` oeffnen (`location.href = buildMailto(...)`), danach Hinweis "ZIP bitte manuell anhaengen".
- [ ] **Step 3:** Nach Versand Rueckfrage "Korb jetzt leeren?" → bei Ja `DB.clearKorb` + Ansicht/Badge aktualisieren.
- [ ] **Step 4:** Sichtpruefung (Smartphone/Desktop): kompletter Durchlauf — Posten erfassen → senden → ZIP-Download + Mailprogramm mit Betreff/Empfaengern/Body → Korb leeren.

---

## Block E — Doku, Feinschliff, Deploy

### Task E1: README.md + CHANGELOG.md

**Files:**
- Create: `README.md`, `CHANGELOG.md`

- [ ] **Step 1:** `README.md` an Vorbild anlehnen: Zweck, Installation aufs Smartphone, Update-/Cache-Verhalten, Datenspeicherung, Ablauf (Katalog → Korb → senden), Stammdaten-Pflege in Excel (importieren/Vorlage), Hinweis "ZIP manuell anhaengen".
- [ ] **Step 2:** `CHANGELOG.md` (E1-Format): `## v0.1.0 (TT.MM.JJJJ, HH:MM Uhr)` — echten Zeitstempel per `Get-Date -Format "dd.MM.yyyy, HH:mm"` holen — mit `### Neu` (Erstversion: Katalog aus Excel, Suche, Korb, Fotos, Nachbestell-Mail).

### Task E2: In-App-Hilfe + UI-Review (Architekt)

**Files:**
- Create: `hilfe/index.html`
- Review: gesamte UI

- [ ] **Step 1:** Kurz-Hilfe `hilfe/index.html` (Vorbild-Stil): 3-Schritt-Anleitung + FAQ (Import, Vorlage, Versand).
- [ ] **Step 2:** Architekt-Review der Oberflaeche: Aufbau, Lesbarkeit, E1-Optik-Paritaet zum Vorbild, Touch-Bedienbarkeit auf dem Smartphone. Befunde einarbeiten.

### Task E3: Git-Repo + GitHub-Pages-Deploy vorbereiten

**Files:**
- Create: `.gitignore` (aus Vorbild), `package.json` (falls gewuenscht), `manifest`/Pfad-Check fuer Pages

- [ ] **Step 1:** `.gitignore` aus Vorbild uebernehmen. `git init`, erste Commits der gesammelten Blocks.
- [ ] **Step 2:** Relative Pfade fuer GitHub-Pages pruefen (alle `./`-relativ, kein absoluter Root).
- [ ] **Step 3:** **Felix:** GitHub-Repo anlegen, pushen, Pages aktivieren, auf Testgeraet installieren und Durchlauf pruefen. (Push/Deploy bleibt bei Felix.)

---

## Self-Review (Plan ↔ Spec)

- **Spec-Abdeckung:** Infrastruktur-Paritaet (A1/A2), Excel-Import + Pflege (B1/B4), volle Liste (B2) + Suche/Autovervollstaendigung (B3) + Freitext (C3), Korb + Menge/Notiz/Foto (C2/C4), Empfaenger waehlbar + Sammel-Mail (D1/D3/D2), ZIP+mailto (D2), YAGNI-Grenzen (keine Bestandsfuehrung/kein In-App-Katalog-Edit) eingehalten. Name "E1 Material" durchgezogen. — Keine Luecke gefunden.
- **Platzhalter:** Zwei bewusst offene Spec-Punkte (ZIP-Format, Hilfe/Schrift) sind in D2-Step1 bzw. E2 mit Entscheidungsregel verankert, nicht als TBD offen.
- **Typ-Konsistenz:** `Item`- und `Posten`-Felder, `Catalog.*`-, `DB.*`-, `Exporter.*`-, `Basket.*`-Signaturen ueber Tasks hinweg einheitlich benannt.

---

## Taskliste mit Modell-Zuordnung

**Stufe → Modell-ID**

| Stufe | Kurzname | Modell-ID |
|---|---|---|
| Planung/Architektur/Review | Opus 4.8 | `claude-opus-4-8` |
| Standard-Implementierung | Sonnet 5.0 | `claude-sonnet-5` |
| Mechanik/Boilerplate | Haiku 4.5 | `claude-haiku-4-5-20251001` |

### Block A — Geruest
| # | Beschreibung | Modell | Begruendung |
|---|---|---|---|
| `[Haiku  4.5]` A1 | Ordner + statische Dateien aus Vorbild kopieren, version.json | Haiku 4.5 (`claude-haiku-4-5-20251001`) | Rein mechanisch: Verzeichnisse, Datei-Kopien, 1-Zeilen-JSON. |
| `[Sonnet 5.0]` A2 | index.html-Shell, style.css, manifest, sw.js anpassen | Sonnet 5.0 (`claude-sonnet-5`) | Mehrere Dateien, SW-Cache-Liste + Ansichts-Geruest koordinieren. |

### Block B — Katalog
| # | Beschreibung | Modell | Begruendung |
|---|---|---|---|
| `[Sonnet 5.0]` B1 | catalog.js Parser + Speicher | Sonnet 5.0 (`claude-sonnet-5`) | Kern-Parselogik mit Randfaellen, Code liegt im Plan vor. |
| `[Sonnet 5.0]` B2 | Katalog-Ansicht gruppiert rendern | Sonnet 5.0 (`claude-sonnet-5`) | DOM-Rendering + Ansichts-Anbindung. |
| `[Sonnet 5.0]` B3 | Suche + Autovervollstaendigung | Sonnet 5.0 (`claude-sonnet-5`) | Umlaut-Normalisierung + Vorschlagslogik, etwas Urteil noetig. |
| `[Sonnet 5.0]` B4 | Import + Vorlage-Download | Sonnet 5.0 (`claude-sonnet-5`) | Datei-I/O, Fehlerbehandlung, Bestaetigungsfluss. |

### Block C — Korb
| # | Beschreibung | Modell | Begruendung |
|---|---|---|---|
| `[Sonnet 5.0]` C1 | db.js IndexedDB-Wrapper | Sonnet 5.0 (`claude-sonnet-5`) | Async-Wrapper nach Vorbild-Muster spiegeln. |
| `[Sonnet 5.0]` C2 | Posten aus Katalog (Menge/Notiz/Foto) | Sonnet 5.0 (`claude-sonnet-5`) | Dialog + Blob-Handling + DB-Anbindung. |
| `[Haiku  4.5]` C3 | Freitext-Posten | Haiku 4.5 (`claude-haiku-4-5-20251001`) | Variante von C2 nach klarer Vorlage — mechanisch. |
| `[Sonnet 5.0]` C4 | Korb-Ansicht + Badge | Sonnet 5.0 (`claude-sonnet-5`) | Render + Edit/Delete + Badge-Kopplung. |

### Block D — Versand
| # | Beschreibung | Modell | Begruendung |
|---|---|---|---|
| `[Sonnet 5.0]` D1 | Monteur-Name + Empfaengerliste | Sonnet 5.0 (`claude-sonnet-5`) | Settings-Persistenz + Erststart-Fluss. |
| `[Sonnet 5.0]` D2 | export.js ZIP + mailto | Sonnet 5.0 (`claude-sonnet-5`) | Zentrale Export-Logik, Vorbild adaptieren. |
| `[Sonnet 5.0]` D3 | Versand-Flow + Korb leeren | Sonnet 5.0 (`claude-sonnet-5`) | Zusammenfuehrung mehrerer Module. |

### Block E — Doku & Deploy
| # | Beschreibung | Modell | Begruendung |
|---|---|---|---|
| `[Sonnet 5.0]` E1 | README + CHANGELOG | Sonnet 5.0 (`claude-sonnet-5`) | Doku nach Vorbild (Dokumentar-nah). |
| `[Sonnet 5.0]` E2a | In-App-Hilfe | Sonnet 5.0 (`claude-sonnet-5`) | Hilfe-Seite nach Vorbild-Stil. |
| `[Opus   4.8]` E2b | UI-/Layout-Review (Architekt) | Opus 4.8 (`claude-opus-4-8`) | Gestaltungs-Urteil, E1-Optik-Paritaet, hohe Stakes. |
| `[Haiku  4.5]` E3a | .gitignore, git init, Pfad-Check | Haiku 4.5 (`claude-haiku-4-5-20251001`) | Mechanik. |
| `[Felix     ]` E3b | GitHub-Repo, Push, Pages, Geraete-Test | Felix | Externes Schreibziel + Hardware-Test bleibt bei Felix. |

### Verteilung
- **Opus 4.8:** 1 Task (E2b — UI-Review)
- **Sonnet 5.0:** 12 Tasks (Kern-Implementierung)
- **Haiku 4.5:** 3 Tasks (A1, C3, E3a — Mechanik)
- **Felix:** 1 Task (E3b — Deploy/Test)

Planung dieser Datei: Opus 4.8 (`claude-opus-4-8`).
